## 🚀 Setup & Run (Bolt)

This project is hosted and runnable on [Bolt](https://bolt.new).

### To Run:
1. Open the project using the secret Bolt link.
2. Click the **Run ▶️** button at the top of the editor.
3. The output will appear in the console below.

> 💡 No installation is needed — Bolt handles dependencies automatically.

if it doesnt not work: use this 

# BoltDesign2 Project

This is a React + Vite project template based on BoltDesign.  
It uses React, TailwindCSS, and Vite for development.

---
## 🚀 Quick Start (Windows)

Open **PowerShell as Administrator** and copy-paste these commands:

```powershell
# 1. Delete old dependencies (if any)
rmdir /s /q node_modules
del package-lock.json

# 2. Update npm (optional, but recommended)
npm install -g npm@latest

# 3. Install all dependencies
npm install

# 4. Start the development server
npm run dev
